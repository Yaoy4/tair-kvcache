"""Sweep figure factories (V2+).

Each public function takes a ``pandas.DataFrame`` produced by
``sweep_data`` and returns a ``plotly.graph_objects.Figure``. No
function in this module performs file IO.

V0 placeholder: contents land in V2.
"""
