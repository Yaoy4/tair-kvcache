"""Sweep data layer (V1+).

This module loads ``summary.csv`` produced by
``aic_topn_to_hisim_sweep.py`` and adds derived columns used by the
figure / report layers.

V0 placeholder: contents land in V1.
"""
